export enum CAMERA_TYPE {
  FRONT = 'front',
  BACK = 'back',
}

export enum CAMERA_FLASH_MODE {
  ON = 'on',
  OFF = 'off',
  AUTO = 'auto',
}

export enum CAMERA_FOCUS_MODE {
  ON = 'on',
  OFF = 'off',
}
